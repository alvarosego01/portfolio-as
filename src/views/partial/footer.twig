{% from 'components/buttons/Button.twig' import Component as Button %}

<footer class="pt-24 pb-4 bg-darkBlue">
	<div class="max-w-6xl px-10 pb-10 mx-auto border-b border-mainWhite">

		<div class="grid grid-cols-1 gap-10 pcTab:gap-6 pcTab:grid-cols-12 ">

      <div class="flex flex-col gap-10 pcTab:justify-evenly pcTab:flex-row pcTab:pb-10 pcTab:col-span-8">

			<div class="">
        <div class="flex flex-row gap-4 socials">
          {% for item in socials %}
          <a href="{{ item.url }}" class="cursor-pointer hover:text-lightSteelBlue">
            <i class="{{ item.properties.icon_type }} text-4xl block"  ></i>
          </a>
          {% endfor %}
        </div>
			</div>
			</div>

		</div>
	</div>
	<div class="flex items-center justify-center w-full max-w-6xl py-6 mx-auto rights">
		<span class="w-full text-base text-center text-mainWhite">
			© 2025 Alvaro Segovia - Portfolio.
		</span>
	</div>

</footer>

{{ function("wp_footer") }}
