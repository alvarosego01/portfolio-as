{% macro Component(param = {
  icon: ''
}) %}

  {% set icon = param.icon %}

{% if icon starts with 'http' %}
  <img src="{{ icon }}" alt="" class="icon">
{% else %}
  {{ icon | raw }}
{% endif %}

{% endmacro %}